# 0 is too far from ` ;)
set -g base-index 1

# Automatically set window title
set-window-option -g automatic-rename on
set-option -g set-titles on

#set -g default-terminal screen-256color
set -g status-keys vi
set -g history-limit 10000

setw -g mode-keys vi
setw -g mouse on
setw -g monitor-activity on

bind-key v split-window -h
bind-key s split-window -v

bind -r H resize-pane -L 1
bind -r J resize-pane -D 1
bind -r K resize-pane -U 1
bind -r L resize-pane -R 1

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D 
bind k select-pane -U
bind l select-pane -R

# Use Alt-vim keys without prefix key to switch panes
bind -n M-h select-pane -L
bind -n M-j select-pane -D 
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# No delay for escape key press
set -sg escape-time 0

# Reload tmux config
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# THEME
set -g status-bg black
set -g status-fg white
set -g window-status-current-bg white
set -g window-status-current-fg black
set -g window-status-current-attr bold
set -g status-interval 60
set -g status-left-length 50
set -g status-left '#[fg=green](#S) #(cd #{pane_current_path}; git rev-parse --abbrev-ref HEAD)'
#set -g status-right '#[fg=yellow]#(cut -d " " -f 1-3 /proc/loadavg)#[default] #[fg=white]%H:%M#[default]'
set -g status-right '#[fg=yellow]#(cut -d " " -f 1-3 /proc/loadavg)#[default] #[fg=white]%H:%M#[default]'


# OLD TMUX 
#set -g default-terminal "screen-256color"
#
#set -g prefix C-a
#unbind C-b
#
#bind C-a send-prefix
#
#set -sg escape-time 1
#
#bind r source-file ~/.tmux.conf \; display "Reloaded!"
#
#bind | split-window -h
#bind - split-window -v
#
#bind -r H resize-pane -L 1
#bind -r J resize-pane -D 1
#bind -r K resize-pane -U 1
#bind -r L resize-pane -R 1
#
#setw -g mode-mouse on
#set -g mouse-select-pane on
#set -g mouse-resize-pane on
#set -g mouse-select-window on
#
#set -g status-fg white
#set -g status-bg black
#
#setw -g window-status-fg cyan
#setw -g window-status-bg default
#setw -g window-status-attr dim
#
#setw -g window-status-current-fg white
#setw -g window-status-current-bg red
#setw -g window-status-current-attr bright
#
#set -g pane-border-fg color
#set -g pane-border-bg color
#
#set -g pane-active-border-fg color
#set -g pane-active-border-bg color
#
#set -g pane-border-fg green
#set -g pane-border-bg black
#set -g pane-active-border-fg white
#set -g pane-active-border-bg yellow
#
#set -g message-fg yellow
#set -g message-bg black
#set -g message-attr bright
#
#set -g status-utf8 on
#
#set -g status-left-length 40
#set -g status-left "#[fg=green]Session: #S, #[fg=yellow]wn: #I, #[fg=cyan]pane: #P"
#
#set -g status-right "#[fg=#00ff7f]#(ruby -v | cut -c -13) #[fg=#ffe4fe]#(rails -v)"
#
#set -g status-utf8 on
#
#set -g status-justify centre
#
#setw -g monitor-activity on
#set -g visual-activity on
#
#setw -g mode-keys vi
#
#unbind [
#bind Escape copy-mode
#unbind p
#bind p paste-buffer
#bind -t vi-copy 'v' begin-selection bind -t vi-copy 'y' copy-selectio:
#
#bind p new-window -d -n tmp \; swap-pane -s tmp.0 \; select-window -t tmp
#bind o last-window \; swap-pane -s tmp.1 \; kill-window -t tmp
